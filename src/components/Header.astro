---
import { SITE_TITLE } from '../consts';
import { withBase } from '../utils';
---

<header class="site-header">
  <p class="site-title"><a href={withBase('/')}>{SITE_TITLE}</a></p>
  <nav>
    <a href={withBase('/')}>Latest</a> · <a href={withBase('/archive')}>Archive</a> ·
    <a href={withBase('/about')}>About</a> · <a href={withBase('/search')}>Search</a>
  </nav>
</header>
