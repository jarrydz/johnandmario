{
  "name": "johnandmario",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro",
    "img": "ANTHROPIC_API_KEY=\"${ANTHROPIC_API_KEY:-$(security find-generic-password -s ANTHROPIC_API_KEY -w 2>/dev/null)}\" node scripts/img.mjs",
    "quote": "node scripts/quote.mjs"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.3.7"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  }
}
